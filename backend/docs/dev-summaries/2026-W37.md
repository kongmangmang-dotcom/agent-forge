# Development summary — 2026-W37

### 2026-09-07

- **摘要**：计划任务支持紧急程度展示/编辑，并新增可自定义任务标签。
- **范围**：agent-forge 日程 API + Schedule 前端；迁移 `013_daily_task_tags.sql`。
- **变更要点**：
  - `daily_task.tags` JSONB；创建/更新/列表/详情/续作透传与归一化。
  - 紧急程度沿用 `priority`（高/中/低），前端创建弹窗与任务要求区可编辑，列表展示徽章。
  - 任务标签支持预设（阻塞/跟进/评审/文档）+ 自定义输入。
- **备注**：API 已重启并验证 create/list/patch；刷新前端即可用。

### 2026-09-07（续）

- **摘要**：新增内置角色 `game_designer`（Game Designer / 游戏设计）。
- **范围**：角色种子、枚举、编排 hint、前端角色展示名。
- **变更要点**：`scripts/seed_roles.py` 已入库；code=`game_designer`，默认 openai；关注玩法/系统/数值/关卡与体验节奏。
