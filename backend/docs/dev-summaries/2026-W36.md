# Development summary — 2026-W36


### 2026-08-31

- **摘要**：工作流支持配置「同 Agent 连续步骤共用长对话」（`reuse_same_agent_session`）。
- **范围**：编排器、工作流 CRUD/表单、dev-plan seed。
- **变更要点**：
  - `orchestrator`：开启后将同 Agent 历史消息注入下一步 prompt（CLI 场景新建 AgentRun + 历史）。
  - 前端新建/编辑勾选；列表展示徽章；`options` 更新改为浅合并。
  - `dev-plan` 默认开启该选项。
- **备注**：真·同一进程 inject 仅当上一步 AgentRun 仍为 running；顺序步骤通常走历史注入。


### 2026-08-31 (requirement)

- **摘要**：今日任务支持短标题 + 详细需求 `requirement`，详情可改并保存。
- **范围**：schedule 模型/API/Schedule 页。
- **变更要点**：migration `003_daily_task_requirement.sql`；`plan_today` 存 requirement 并派生 title；启动工作流优先 requirement；详情页「任务要求」卡片。
- **备注**：需重启无 reload 的后端进程。


### 2026-08-31 (start-workflow UX)

- **摘要**：修复「启动工作流」看起来只跳转的问题：带上 `run` 查询参数并在工作流页挂载轮询。
- **范围**：Schedule / Workflows 前端。
- **变更要点**：后端其实已创建 WorkflowRun；前端原先只跳 `?def=` 未带 run id。


### 2026-08-31 (schedule notes/memory/date)

- **摘要**：Schedule 支持按日期筛选与日总览；任务可存笔记/附件路径与 Agent 记忆，启动工作流时注入 prompt。
- **范围**：migration `004`、schedule API/UI。
- **变更要点**：`GET /schedule/overview`；notes/memories CRUD；详情双栏面板。


### 2026-08-31 (progress overview)

- **摘要**：新增「完成总览」页，按天展示任务完成数/完成率与步骤进度。
- **范围**：`GET /schedule/overview-range`、前端 `/progress` 导航。


### 2026-08-31 (run monitor workflows)

- **摘要**：运行监控改为只展示 WorkflowRun 列表与步骤进度，可下钻 Agent Run。
- **范围**：`RunMonitor.tsx`。


### 2026-08-31 (workflow step live panel)

- **摘要**：工作流页点击步骤后，下方展示侧栏元信息 + Agent 实时流程（对话/文件/日志）。
- **范围**：`Workflows.tsx` + `LiveRunPanel`。


### 2026-08-31 (auto md notes)

- **摘要**：Workflow 关联任务下，AgentRun 完成时自动把生成的 `.md` 登记到任务「笔记与文档」。
- **范围**：`task_note_sync`；在 `run_service` / `orchestrator` 完成时触发；按 `file_path` 去重更新。


### 2026-08-31 (terminate workflow)

- **摘要**：运行监控支持删除/终止 WorkflowRun；有关联文档时确认保留或删除。
- **范围**：`terminate-preview` / `terminate` API；RunMonitor 确认弹窗。


### 2026-08-31 (fix terminate FK)

- **摘要**：修复终止并删除 WorkflowRun 时 500（`agent_run.step_run_id` 外键阻挡级联删除 `step_run`）。
- **范围**：`orchestrator.terminate_workflow` 删除前先把相关 `agent_run.step_run_id` 置空。


### 2026-08-31 (fix codex turn.completed)

- **摘要**：Codex `turn.completed` 后若 CLI 卡在插件同步等收尾，Run 会一直 running；现映射为完成并强停进程树。
- **范围**：`codex_event_mapper`、`cli_coding._stop_process`；单测 `test_codex_event_mapper`。


### 2026-08-31 (plan progress / multi-bind / note preview)

- **摘要**：开发计划表状态与 WorkflowRun 步骤同步；任务可绑定多个工作流但同时仅一个运行；笔记支持预览弹窗。
- **范围**：`plan_progress_sync`、migration `005`、`Schedule.tsx`。


### 2026-08-31 (per-workflow plan tables)

- **摘要**：Schedule 按绑定工作流分别展示开发计划表；支持新建工作流按钮；计划项带 `workflow_definition_id`。
- **范围**：migration `006`、`workflow_plans` API、`Schedule.tsx` 分区 UI。


### 2026-08-31 (start workflow with md docs)

- **摘要**：启动任务工作流时默认把已有 Markdown 笔记/文档全文注入 prompt；优先用笔记 body，否则按路径读盘；工作区取步骤 Agent 的 workspace。
- **范围**：`schedule_service._build_start_prompt`。


### 2026-08-31 (markdown note preview)

- **摘要**：笔记预览改为正式 Markdown 渲染（标题/列表/表格），并支持预览/源码切换。
- **范围**：`MarkdownPreview.tsx` + Schedule 预览弹窗。


### 2026-08-31 (task agent chat)

- **摘要**：今日任务可直接指定 Agent 对话实现功能；首轮附带已有 Markdown 文档，后续同会话不再重复附带。
- **范围**：schedule API/UI、`agent_run.daily_task_id`、LiveRunPanel 可自定义发送。
- **变更要点**：
  - migration `007_agent_run_daily_task.sql`；`POST /schedule/tasks/{id}/agent-chat`（start / inject / continue）。
  - Codex 等 `uses_stdin=False`：运行中不可 inject，结束后以历史续聊且不带文档。
  - Schedule 页「Agent 对话」为右下角悬浮窗：可收起为胶囊、可关闭；顶栏按钮打开。
- **备注**：后端无 reload 时需重启 uvicorn。


### 2026-09-01 (codex long prompt)

- **摘要**：修复 Codex 附带长文档时 Windows 命令行过长秒失败；文档改为写入工作区文件并由 stdin 传 prompt。
- **范围**：`cli_coding.CodexCliProvider`、`schedule_service._build_start_prompt`。
- **变更要点**：
  - Codex `exec` 使用 `-` + stdin 喂初始 prompt（不再塞 argv）。
  - 任务 Markdown 笔记物化到 `.agentforge/task-docs/{task_id}/`，prompt 只引用路径。
  - Windows stderr 优先按 gbk 解码，避免「命令行太长了」乱码。


### 2026-09-02 (knowledge RAG MVP)

- **摘要**：在 AgentForge 落地精简版知识库（Python）：建库、文本入库、分块、向量化、检索；不含租户/配额/RBAC。
- **范围**：`app/api/v1/knowledge.py`、`knowledge_service.py`、`knowledge_chunk.py`、`knowledge_embed.py`、migration `010_knowledge_base.sql`。
- **变更要点**：
  - 向量存 Postgres JSONB；可选 OpenAI-compatible embedding，默认 `local_hash`。
  - API：`/api/v1/knowledge/bases`、documents、search、reindex。
- **备注**：无 PDF/Tika；工作流 prompt 尚未注入。


### 2026-09-02 (knowledge chat UI)

- **摘要**：知识库页改为 Cursor 风格问答主界面；右侧小页切换「知识库 / 文件」；新增 `POST .../chat` RAG 问答。
- **范围**：前端 `Knowledge.tsx`（full-bleed）、后端 `knowledge_chat.py` + `KnowledgeService.chat`。
- **备注**：未配 Chat API 时返回摘录整理；配置 `CHAT_API_*` 后合成自然语言回答。

